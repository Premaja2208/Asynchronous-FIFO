# Asynchronous-FIFO
The asynchronous FIFO enables safe data transfer between independent write (wr_clk) and read (rd_clk) clock domains. Gray-code pointer synchronization prevents metastability, while full and empty flags ensure correct FIFO operation.
